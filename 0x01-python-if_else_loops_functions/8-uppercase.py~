#!/usr/bin/python3

def islower(a):
    if ord(a) in range(ord('a'), ord('z')):
        return True
    else:
        return False
def uppercase(str):
    for i in range(0, len(str)):
        print("{}",.format(str[i] if islower(str[i]) else chr(ord(str[i]) + 32) ))
